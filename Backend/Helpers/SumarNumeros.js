//Está funcion recibe dos numeros y devuelve la suma de ellos
function SumarNumeros(a, b) {
  return parseInt(a) + parseInt(b);
}

function MultiplicarX2(a) {
  return parseInt(a) * 2;
}

export { SumarNumeros };